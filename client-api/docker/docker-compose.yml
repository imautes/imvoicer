version: '3.3'
services:
  mysql:
    env_file:
      - db.env
    container_name: ${MYSQL_CONTAINER}
    image: mysql
    restart: always
    ports:
      - '3306:3306'
    expose:
      - '3306'
    volumes:
      - db:/var/lib/mysql
volumes:
  db:
